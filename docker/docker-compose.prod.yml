# Production override for AMD64 deployment target
version: '3.8'

services:
  train-status:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        TARGETPLATFORM: linux/amd64
    image: train-status:latest-amd64
