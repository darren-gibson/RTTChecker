# Development override for local Mac/ARM64 environment
version: '3.8'

services:
  train-status:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        TARGETPLATFORM: linux/arm64
    image: train-status:latest-arm64
    env_file:
      - ../.env
