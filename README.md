# RTT Checker

Small Node.js bridge to the RTT API. The core logic was refactored into `src/RTTBridge.js` and unit tests were added using Jest.

Install and run tests:

```bash
npm install
npm test
```

Start server:

```bash
npm start
```
